{
  "name": "diario-pratica-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker - API proxy para OpenAI no Diário de Prática Espiritual",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
